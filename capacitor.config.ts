import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.udemy.course.io.ionic.starter.recipes',
  appName: 'ionic-angular-course',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
